# Configuration Files

Real WireGuard configuration files are intentionally excluded.

WireGuard configuration files contain private cryptographic keys and should not be published publicly.

The repository only contains sanitized examples to demonstrate the configuration structure.
